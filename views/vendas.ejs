<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transações</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">    <style>
        .total-positive { color: green; }
        .table-hover tbody tr:hover { background-color: #f8f9fa; }
        .transaction-id { font-weight: bold; }
    </style>
</head>
<body class="container mt-5">
    <nav class="navbar navbar-dark bg-dark mb-4">
        <div class="container">
            <span class="navbar-brand mb-0 h1">Controle de Transações</span>
        </div>
    </nav>

    <div class="card shadow">
        <div class="card-header bg-primary text-white">
            <h2 class="mb-0">Últimas Transações</h2>
        </div>
        
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Aluno</th>
                            <th>Item</th>
                            <th>Quantidade</th>
                            <th>Total</th>
                            <th>Data/Hora</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% transacoes.forEach(transacao => { %>
                            <tr>
                                <td class="transaction-id"><%= transacao.id %></td>
                                <td><%= transacao.Aluno.nome %></td>
                                <td><%= transacao.ItemLanchonete.nome %></td>
                                <td><%= transacao.quantidade %></td>
                                <td class="total-positive">R$ <%= transacao.total.toFixed(2) %></td>
                                <td>
                                    <%= new Date(transacao.createdAt).toLocaleString('pt-BR') %>
                                   
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>

            <% if (transacoes.length === 0) { %>
                <div class="alert alert-info text-center">Nenhuma transação registrada</div>
            <% } %>
        </div>
    </div>

    <footer class="mt-5 text-center text-muted">
        Sistema de Lanchonete - © <%= new Date().getFullYear() %>
    </footer>
</body>
</html>